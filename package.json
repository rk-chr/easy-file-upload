{
  "name": "easy-file-upload",
  "version": "1.0.0",
  "description": "easy-file-upload is a node js middleware which is used multer library for uploading files easily with just one function for handling `multipart/form-data`.",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon -r --ignore '*.json'",
    "start": "NODE_ENV=production node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ramki2486/easy-file-upload.git"
  },
  "keywords": [
    "easy-file-upload",
    "easy file upload",
    "multer",
    "file upload",
    "upload",
    "file"
  ],
  "author": "Rama Kishore (https://github.com/ramki2486)",
  "license": "ISC",
  "homepage": "https://github.com/ramki2486/easy-file-upload#readme",
  "dependencies": {
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "eslint": "^5.3.0",
    "eslint-config-airbnb-base": "^13.2.0",
    "eslint-plugin-import": "^2.18.2",
    "nodemon": "^1.19.1"
  }
}
